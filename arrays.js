// Arrays
// Un array o arreglo es una lista homogenear de algo en particular.
// Su indice comienza en 0
const numeros = ['a', 'b', 'c'];

// Para acceder a un elemento del array se utiliza su indice
// Ejemplo: Acceder a la letra a en el array utilizando el indice 0
console.log(numeros[0]);